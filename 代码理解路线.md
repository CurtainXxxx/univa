# UniVA 代码理解路线（明天组会前完成）

> 目标：把一条完整调用链读通，能合上文件画出来、能回答老师细节提问。
> 时长：2-3 小时。方法：**从入口跟调用链，只读核心文件，读一段问自己"它回答什么问题"**。

---

## 第 0 步：先跑起来（10 分钟）

你已经跑过了，但为了建立"入口感"：

```
cd D:\univa\univa
python univa_agent.py      # 交互式 CLI，输入 "生成一个小猫跳舞视频"
```

看着它执行，心里记下：你输入了什么 → 屏幕上先出现什么（计划）→ 再出现什么（执行）→ 最后输出什么（JSON 结果）。

---

## 第 1 步：读主进程 univa_agent.py（60 分钟，最重要）

文件：`D:\univa\univa\univa_agent.py`（约 600 行）

**读的顺序（从上往下）**：

| 读什么 | 回答什么问题 | 关键行 |
|---|---|---|
| `load_prompt()` | 提示词从哪来？ | plan.txt 在 prompts/ 目录 |
| `PlanAgent.__init__` | 规划 Agent 有什么工具？**注意：没有 tools** | 为什么它不能调工具 |
| `PlanAgent.generate_plan()` | 计划怎么生成的？返回什么格式 | JSON：execution_plan.steps[] |
| `extract_plan_from_content()` | LLM 输出乱时怎么兜底？ | 正则找 ```json |
| `ActAgent.__init__` | 执行 Agent 有什么？**tools=[mcp_tools]** + `tool_call_limit=1` | 每步只调 1 个工具 |
| `ActAgent._execute_step()` | 一步怎么执行？ | 上下文含：问题+计划+已完成步骤+当前步骤 |
| `PlanActSystem.execute_task()` | 整体流程 | plan → execute → inject 记忆 |
| `inject_execution_results()` | "记忆"存在哪？ | **SQLite**（plan_act_system_plan.db） |
| `SingleAgent`（我们新加的） | 对照组怎么设计的 | 一个 Agent 同时规划+执行 |

**读完合上文件，自测**：在纸上画出来：
```
用户输入 → PlanAgent(无工具,输出JSON计划) → ActAgent(每步调1个MCP工具) → 结果回写 → 存进SQLite
```

---

## 第 2 步：读一个工具的全链路（40 分钟）

文件：`D:\univa\univa\mcp_tools\video_gen.py`（你最熟的，改过它）

**只读 text2video_gen 这一个函数**，然后追下去：

| 文件 | 看什么 |
|---|---|
| `mcp_tools/video_gen.py` | `text2video_gen()` — 读配置 → 调 API → 返回 dict |
| `utils/wavespeed_api.py` | `text_to_video_generate()` — **提交任务 + 轮询 + 下载** 三段式 |
| `config/mcp_tools_config/config.yaml` | model 字段怎么决定走哪个 API（seedance/wan_api） |
| `mcp_tools/base.py` | ToolResponse 是什么（所有工具的统一返回格式） |

**自测**：回答"视频生成的完整链路"：MCP server 收到调用 → 读 config → POST 提交任务 → 轮询 status → 下载 mp4 → 返回路径。

---

## 第 3 步：读你自己的代码（30 分钟）

文件：`D:\univa\eval\runner.py` + `D:\univa\eval\metrics.py`

- runner：三种模式（agent/single/direct）各自干什么？成功判定怎么写的（lvu 看 message，生成类看 output_path）？
- metrics：CLIP 怎么抽帧打分？DINO 怎么对比两个视频？

---

## 第 4 步：查漏（20 分钟）

| 问题 | 去哪里找答案 |
|---|---|
| MCP 是什么？工具怎么注册成 server？ | `mcp_configs.json`（5 个 server 的启动命令）+ `@mcp.tool()` 装饰器 |
| 工具跑在子进程里？ | 每个 mcp_tools/*.py 结尾的 `mcp.run(transport="stdio")` |
| 视觉理解怎么抽帧？ | `query_llm.py` 的 `_extract_and_encode_video_frames`（128/64 帧 → base64） |
| API key 怎么覆盖？ | 每个工具文件头部的 `.env` 加载 + config 覆盖段 |
| 长视频编辑怎么分片？ | `video_editing.py` 的 `long_video_edit`（我们重写的） |

---

## 组会提问自查（答不上来的回去再看）

1. PlanAgent 和 ActAgent 各绑了什么工具？为什么？（规划不需要工具，执行需要）
2. ActAgent 为什么 tool_call_limit=1？
3. 工具成功/失败怎么返回？失败时 Agent 能看到什么？
4. "记忆"存在哪里？什么格式？
5. MCP 子进程和主进程怎么通信？（stdio + JSON）
6. Wavespeed 为什么是"提交+轮询"而不是同步返回？（异步任务）
7. 为什么 lvu 要抽帧？为什么 64 帧而不是 256？（接口限制 250 图 + 成本）
8. long_video_edit 为什么分片？失败片为什么用原片顶替？
9. SingleAgent 和 Plan-Act 的差异最小化了哪些变量？（同模型同工具同记忆，只差分工）
10. CLIP 和 DINO 各衡量什么？（语义对齐 / 内容保留）

---

## 快速技巧

- VS Code 里 `F12` 跳转到函数定义，跟着调用链走（这个项目跳转很干净）
- 每个文件先看 import（依赖谁）+ 函数名列表（做什么），再看实现
- 你改过的代码是你最熟的：video_gen.py（entity2video）、video_editing.py（long_video_edit）、univa_agent.py（SingleAgent）——这三段能讲 5 分钟细节，组会够了
- **组会 80% 的问题会集中在"你改过/写过的东西"上**：runner、long_video_edit、SingleAgent、评测口径——这些你本来就很懂，重点把它们讲清楚
