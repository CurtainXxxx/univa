# UniVA 复现工作 · 组会汇报 PPT 提纲

> 汇报定位：**讲自己做了什么**（不是讲论文）。主线 = 我搭了一套能跑的复现系统，跑通了三类任务，拿到接近论文的结果，下一步做消融对照。
> 时长建议 15 分钟，10 页，最后 1 页留作提问缓冲。

---

## P1 封面（30 秒）

**标题**：UniVA（通用视频 Agent）论文复现实验进展

**要点**：
- 复现目标：验证论文三组核心结论
- 一句话：已完成评测框架搭建 + 三类任务链路跑通 + lvu 理解任务基本复现（74.8% vs 论文 76%）

**讲稿**：大家好，我最近的工作是复现 UniVA 这篇论文（通用视频 Agent），目标是验证论文的结论能不能被独立复现，目前进展到第二阶段——链路全通、第一批指标已经出来。

---

## P2 论文是什么（30 秒，别多讲）

**标题**：UniVA 在解决什么问题

**要点**（3 行）：
- 一个 Agent 完成**四类视频任务**：生成 / 编辑 / 理解 / 分割
- 核心设计 ①：**Plan-Act 双 Agent** 分工（规划 Agent 拆计划，执行 Agent 调工具）
- 核心设计 ②：**三层记忆**（Task/User/Global）让 Agent 越用越懂你
- 实现方式：工具全部 **MCP 协议化**（视频生成、编辑、理解都是独立可插拔工具）

**讲稿**：论文的核心是把多模态工具（生成/编辑/理解）统一成 MCP 工具，交给两个 Agent 分工协作。我复现的就是这套架构。

---

## P3 我怎么复现的（2 分钟，重点）

**标题**：复现路线图

**要点**：
- 环境：Windows 本地（CPU）+ Wavespeed API（视频生成）+ 阿里 DashScope qwen-vl-max（视觉理解）
- 数据集：UniVA-Bench（4GB，6 类任务 × 10 条）完整下载，还修了 1 处数据 bug
- 评测框架：自己写 runner（任务执行器）+ 4 个指标（CLIP / DINO / MLLM Judge / QA 准确率）

**配图建议**：画一张流水线图（数据集 → runner → Agent/直连 → API → 指标）

**讲稿**：我搭了完整的评测流水线——数据集在本地，runner 负责逐条跑任务，指标分四类：生成任务看 CLIP 对齐、编辑任务看 DINO 保留度、理解任务看 QA 准确率。

---

## P4 我对系统架构的理解（2 分钟，展示你读懂了代码）

**标题**：UniVA 系统怎么跑起来的（代码层面）

**要点**（这是你"读懂了代码"的证据）：
- 用户输入 → **PlanAgent**（只读 LLM，输出 JSON 执行计划）→ **ActAgent**（绑 MCP 工具，每步只调 1 次工具）→ 工具返回路径 → 计划更新 → 记忆回写
- 工具是独立子进程（MCP stdio 协议），5 个 server：video_gen / image_gen / video_editing / video_understanding / audio_gen
- 关键代码位置：`univa_agent.py`（双 Agent 逻辑）、`mcp_tools/`（工具实现）、`utils/wavespeed_api.py`（API 封装）

**配图建议**：架构图（User → PlanAgent → ActAgent → MCP Tools → Wavespeed/DashScope）

**讲稿**：我完整读了一遍系统代码，核心链路是：PlanAgent 把"生成一个小猫视频"这种请求拆成 JSON 计划，ActAgent 按计划调 MCP 工具，工具通过 stdio 协议跑在独立子进程里，我这次工作的很大一部分就是在这一层修问题和加能力。

---

## P5 实验结果（3 分钟，全场重点）

**标题**：目前复现结果

**表格**：

| 任务类 | 状态 | 结果 |
|---|---|---|
| 生成（lt2v/it2v/entity2video） | ✅ 链路全通 | 多图故事 15 秒成片 |
| 理解（lvu） | ✅ 基本复现 | **74.8%**（103 题）vs 论文 **76%** |
| 编辑（lve/v2v 长视频） | ⚠️ 链路通，样本少 | 分片编辑 61s 成片（成本高） |
| 分割（seg） | ❌ 未做 | 需 GPU（计划 AutoDL） |

**配图建议**：一张对比柱状图（InternVL3 75% / UniVA 76% / 我们 74.8%）

**讲稿**（重点讲 lvu 这段，最有说服力）：
> 理解任务我做了全量评测——10 个长视频 103 道题，准确率 74.8%，论文报的 UniVA 是 76%，基本一致。这个过程中我踩了个重要坑：一开始只测每条视频第一问，只有 60%，后来发现数据集每题 13 问、且帧数太少会丢信息，改成全 QA + 64 帧后到 74.8%——**评测口径直接决定结论**，这也说明论文的数字是严格口径下出来的。

---

## P6 工程挑战（3 分钟，讲你"做了什么"的高光）

**标题**：复现过程中解决的三个真实问题

**要点**（选 3 个讲，每个 30 秒）：
1. **API 稳定性**：Wavespeed 轮询会 SSL 断连（生成成功但查询失败）→ 加了指数退避重试 + 绕过本地代理，跑大批量不再崩
2. **视觉理解接入**：DashScope 单次请求限 250 张图 → 抽帧数从 256 压到 128，模型才接得住
3. **长视频编辑**：VACE 模型一次只能编 5 秒 → 自己实现"分片→逐片编辑→合并"管线，59 秒视频出 61 秒成片；顺带修了 3 个 bug（扩展名、时长下限、时间轴丢段）

**讲稿**：复现论文不是跑通就完事，这三类问题是所有复现工作都会遇到的——网络层、接口约束层、模型能力层。

---

## P7 成本与效率（1 分钟，诚实但有亮点）

**标题**：成本控制（复现的现实约束）

**要点**：
- 实测单价：VACE 编辑 ~$0.5/次，比预估贵 3-5 倍；文生视频 ~$0.3/次
- 对策：长视频改用**抽样编辑**（5 分钟视频只编 6 个代表窗口，其余保留原片），成本从 $30/条降到 $3/条
- 总花费至今 ~$4.5，换来了：2 类任务完整链路 + 1 类任务全量指标

**讲稿**：真实 API 计费比论文里"示意图"贵得多，成本控制本身也是复现工作的研究内容——抽样策略就是一个典型的工程-科研权衡。

---

## P8 下一步计划（1 分钟）

**标题**：接下来做什么

**要点**：
- **结论 2**：Single-Agent vs Plan-Act 对照实验（代码已写好，等充值跑小样本）
- **结论 3**：三层记忆消融（Task/User/Global 5 种组合）
- 全量 57 条任务跑完，出完整指标汇总表
- 可选：AutoDL 租 3090 跑分割任务

---

## P9 总结（30 秒）

**标题**：一句话总结

- 复现了论文核心结论 ①（四类任务中的三类已跑通，理解任务数字基本对齐）
- 结论 ②③（双 Agent 有效性、记忆有效性）的对照实验已就绪，待执行
- 复现论文 = 搭框架 + 修工程 + 控成本，我正在走完整条路

---

## 老师可能问的问题（提前准备）

| 问题 | 怎么答（都是你真做过的） |
|---|---|
| **UniVA 的核心贡献是什么？** | 三个：① 用 MCP 把四类视频工具统一成 Agent 可调用的生态；② Plan-Act 双 Agent 分工；③ 三层记忆。我做的是验证这些设计是否真的有效 |
| **你的 74.8% 和论文 76% 差在哪？** | 三个原因：模型不同（我用 qwen-vl-max，论文用 InternVL3-38B）；评测实现是我自己写的；帧采样 64 帧 vs 论文未知。差 1.2pp 在可解释范围内 |
| **为什么选 qwen-vl-max 不用论文的模型？** | 本地无 GPU，论文模型本地跑不了；qwen-vl-max 是 API 接入成本最低的视觉大模型；论文结论的验证不依赖特定模型 |
| **评测口径怎么保证和论文一致？** | 我按数据集原始设计：全 QA（不是抽样）、64 帧均匀采样、选择题字母匹配 + 简答题 LLM judge。论文没开源评测代码，这是我能做的最贴近口径 |
| **双 Agent 实验为什么还没结果？** | 代码已完成（SingleAgent 对照已实现），实验卡在 API 余额和一次环境问题（agno 版本），都已修复，这周跑小样本 |
| **你觉得 Plan-Act 真比 Single 好？** | 论文的观点是分工让规划更稳、执行可验证。我会用同样模型同样工具做对照实验来验证，而不是背书 |
| **你预期 Memory 消融什么结果？** | 论文报的是全开最好（Task+User+Global）。我的设计：User 记忆注入用户偏好、Global 记忆用检索注入领域知识，5 种组合做对比 |
| **分割任务为什么没做？** | 需要本地 GPU 跑 Sa2VA 模型，我的机器是 CPU 环境。计划租 AutoDL 3090 跑，这是最可行的方案 |
| **总共花了多少钱？** | 目前 ~$4.5（折合人民币 30 元左右）。大头是视频编辑（单次 $0.5），已用抽样策略控制 |
| **代码你自己写的部分有多少？** | 评测框架（runner + 指标 + 执行器）全部自己写；系统本体（Agent + 工具）读透后做了三处修复加固：API 重试、视觉帧数适配、长视频编辑工具重写 |

---

## 汇报当天提醒

1. 腾讯会议**共享屏幕前先打开** `UniVA_实验进度.html` 存个档（有 12 页进展图，可当备用素材）
2. 准备 2 个视频演示（选 1 个就好）：
   - `results/long_video_edit/.../0807211316_output.mp4`（lve 61s 编辑成片）
   - `eval/outputs/single_test1/lt2v/lt_0_generated.mp4`（5s 文生视频）
3. 被问"代码理解"类问题就翻 P4——你确实读过：`univa_agent.py` 的 PlanAgent/ActAgent、`mcp_tools/` 的工具、`wavespeed_api.py` 的封装，能说出调用链
4. 不确定的问题就说"这个我还没验证到，计划里安排了"，比硬答好
