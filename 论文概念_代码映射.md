# UniVA：论文概念 ↔ 代码位置映射

> 用法：先纵向读"论文概念"列，每读一个概念就对照右边代码位置去翻一遍；
> 能"概念→代码"和"代码→概念"双向翻译，就是真懂。
> 标注 ⚠️ 的：论文描述了但代码没实现——这是复现工作的 GAP，组会提问时是加分点。

---

## 一、核心架构映射（组会 80% 的问题在这张表里）

| 论文概念 | 论文里的说法 | 代码位置 | 关键符号 | 备注 |
|---|---|---|---|---|
| **Plan-Act 双 Agent** | 规划 Agent 拆计划，执行 Agent 调工具 | `univa/univa_agent.py` | `PlanAgent` / `ActAgent` 两个类 | 本项目的骨架 |
| 规划 Agent | "planning without tool access" | `univa_agent.py` `PlanAgent.__init__` | 构造时**不传 tools** | 论文核心设计：规划不碰工具 |
| 执行 Agent | "acting with tool access" | `univa_agent.py` `ActAgent.__init__` | `tools=[mcp_tools]` + `tool_call_limit=1` | 每步只调 1 个工具 |
| 双 Agent 协作流程 | 先规划→逐步骤执行→结果回写 | `PlanActSystem.execute_task` | 3 行主流程 | 见下节 |
| **工具生态（MCP）** | 四类视频任务工具统一 | `univa/config/mcp_configs.json` | 5 个 server | server 是子进程 |
| 工具注册 | "@mcp.tool()" | 每个 `univa/mcp_tools/*.py` | `@mcp.tool()` 装饰器 | 装饰器=暴露给 Agent |
| 工具子进程通信 | "tool execution as subprocess" | 每个工具文件结尾 | `mcp.run(transport="stdio")` | stdio + JSON |
| **三层记忆** | Task / User / Global | 只有 Task 层实现了 | ⚠️ 见下节 | User/Global 是论文描述 |
| 任务级记忆 | "memory of current task" | `univa_agent.py` | `SqliteDb` + `inject_execution_results` | 存在 `plan_act_system_plan.db` |
| 计划格式 | "structured plan JSON" | `univa_agent.py` | `execution_plan.steps[]` | `extract_plan_from_content` 兜底 |
| 提示词 | "role prompts" | `load_prompt("plan")` | `prompts/plan.txt` | 规划格式约束 |

## 二、Plan-Act 协作流程（背下来，能画图）

```
用户请求
   │
   ▼
PlanAgent.generate_plan()      ← 无工具，纯 LLM，输出 JSON 计划
   │  execution_plan.steps[]（每步: 动作描述 + 工具 + 参数）
   ▼
ActAgent.execute_plan()        ← 绑 MCP 工具，循环每个 step
   │  每步: _execute_step() → 调 1 个工具 → extract_json 解析结果
   │  update_plan() 把 status/output 写回计划
   ▼
inject_execution_results()     ← 结果存进 SQLite（下轮规划会带上）
```

**对应组会问题**："记忆存在哪？" → `SqliteDb(db_file=..._plan.db)` + `session_state`。
**对应组会问题**："ActAgent 为什么每步 limit=1？" → 一次只做一个动作，结果可验证。

## 三、工具层映射（你改过的地方，重点讲）

| 论文概念 | 代码位置 | 你在里面做了什么 |
|---|---|---|
| 文本→视频生成 | `mcp_tools/video_gen.py` → `text2video_gen` | 修了返回类型注解 bug |
| 多图故事生成 | `mcp_tools/video_gen.py` → `entity2video` | 修了故事板幻觉 + 验证完整链路 |
| 长视频编辑 | `mcp_tools/video_editing.py` → `long_video_edit` | **重写**（分片→VACE→合并） |
| 视频理解 | `mcp_tools/video_understanding.py` → `vision2text_gen` | 帧数 256→128 |
| API 封装（提交/轮询/下载） | `utils/wavespeed_api.py` | 加 SSL 重试 + 绕过代理 |
| 视觉 LLM 调用 | `utils/query_llm.py` → `multimodal_query` | 接 DashScope qwen-vl-max |
| 工具统一返回格式 | `mcp_tools/base.py` → `ToolResponse` | 了解即可 |

## 四、⚠️ 论文有但代码没有的（组会加分点）

| 论文概念 | 论文描述 | 代码现状 | 你该怎么说 |
|---|---|---|---|
| **User Memory** | 记住用户偏好，注入下次规划 | ❌ 未实现 | "论文设计了 User/Global 两层记忆，原仓库只实现了 Task 层——这正是我要做消融实验的地方：补齐两层再对比" |
| **Global Memory** | 领域知识检索注入 | ❌ 未实现 | 同上 |
| 分割任务 Sa2VA | 视频目标分割 | 需 GPU，本地没跑 | "需要本地 GPU，计划租 AutoDL" |

**这组 GAP 是组会最亮的点**：说明你不是"照着跑了一遍"，而是**逐条核对了论文设计与实现的差距**——这是复现工作的独立价值。

## 五、怎么自己建立映射（方法论，以后读任何论文都用得上）

**入口 1：概念 → 代码**
看到论文术语，猜代码里的命名，然后搜索：
- "tool" → `tools`、`mcp`、`@mcp.tool()`
- "memory" → `session_state`、`SqliteDb`、`inject_execution_results`
- "plan" → `execution_plan`、`generate_plan`、`plan.txt`
- "act/execute" → `execute_plan`、`_execute_step`
命名通常直译，搜关键词最快。

**入口 2：代码 → 概念**
读一个文件时，每读完一个类/函数，问自己："这段实现对应论文里的哪句话？"
- 看到 `PlanAgent` 没绑工具 → 对应论文 "planning module does not have access to tools"
- 看到 `inject_execution_results` → 对应论文 "store execution results for future planning"

**入口 3：双向验证**
挑 3 个概念，正着讲一遍（概念→代码）再反着讲一遍（代码→概念）。
能双向 = 理解成立。卡住的地方就是你要补读的。

---

## 六、明天之前的最低完成度

- [ ] 背下第二节"协作流程"图（能画出来）
- [ ] 第一节表格里"代码位置"全部能指出来
- [ ] 第四节 GAP 能讲 1 分钟
- [ ] 用第五节方法，给你自己写的 `eval/runner.py` 做一张"概念→代码"小表（runner 对应论文的哪个环节？答案：评测环节，论文没细讲，这是你独立贡献）
